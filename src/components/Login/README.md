# Auth Store - Новый Store для авторизации

## 📋 Описание

Новый zustand store (`authStore.ts`) создан специально для управления только авторизационными данными и токеном. Это позволяет отделить логику авторизации от остальной функциональности приложения.

## 🎯 Что хранится в authStore

- **auth** - статус авторизации (boolean)
- **reg** - режим регистрации (boolean)
- **user** - данные пользователя (AuthUser | null)
- **token** - токен доступа (string | null)
- **phone** - телефон пользователя (string | null)
- **isLoading** - состояние загрузки (boolean)
- **error** - ошибка (string | null)

## 🔧 Методы authStore

### Авторизация
- `login(phone, password)` - вход в систему
- `create(phone, name, terms)` - регистрация
- `restore(phone)` - восстановление пароля
- `compare(sms)` - проверка SMS кода
- `password(password)` - смена пароля
- `logout()` - выход из системы

### Управление состоянием
- `setAuth(auth)` - установить статус авторизации
- `setReg(reg)` - установить режим регистрации
- `setUser(user)` - установить данные пользователя

## 🔑 Особенности

### Автоматическое сохранение токена
При успешной авторизации токен автоматически сохраняется в `localStorage` и восстанавливается при инициализации store.

### Автоматическое сохранение учетных данных
При успешном логине телефон и пароль сохраняются в `localStorage` для автоматического входа.

### Автоматическая очистка
При выходе из системы (`logout()`) автоматически очищаются:
- Токен из localStorage
- Учетные данные из localStorage
- Все данные из store

## 📦 Экспортируемые селекторы

Для оптимизации ре-рендеров используются селекторы:

```typescript
import { useToken, useAuth, useAuthUser, useAuthLoading, useAuthError } from './authStore';

const token = useToken(); // только токен
const isAuth = useAuth(); // только статус авторизации
const user = useAuthUser(); // только данные пользователя
```

## 🔄 Миграция с loginStore

### Что уже мигрировано:
- ✅ `useLogin.ts` - теперь использует `authStore`
- ✅ Логика авторизации полностью в `authStore`

### Что осталось в старом loginStore:
- `getProfile()` - получение полного профиля
- `updateProfile()` - обновление профиля
- Полный интерфейс `User` с дополнительными полями

### План дальнейшей миграции:
1. Создать отдельный `profileStore` для работы с профилем
2. Постепенно мигрировать компоненты с `useLoginStore` на `useAuthStore`
3. Удалить старый `loginStore` после полной миграции

## 📝 Пример использования

```typescript
import { useAuthStore, useToken } from './authStore';

function MyComponent() {
  const { login, auth, isLoading } = useAuthStore();
  const token = useToken();

  const handleLogin = async () => {
    const result = await login('+79001234567', 'password');
    if (!result.error) {
      console.log('Успешная авторизация');
    }
  };

  return (
    <div>
      {auth ? 'Авторизован' : 'Не авторизован'}
      {token && <p>Токен: {token}</p>}
    </div>
  );
}
```

## 🔐 Безопасность

- Токен хранится в localStorage (можно рассмотреть более безопасные варианты)
- Пароль НЕ хранится в store, только в localStorage для автовхода
- При выходе все данные полностью очищаются
